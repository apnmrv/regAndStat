{% extends 'base.html.twig' %}

{% block title %}Admin zone {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <h1>Congrats, u r at the right place now !</h1>

    <h2>Hence come out and do your job :</h2>

    <ul>
        <li><code><a href="{{ path ('this-month-registrations') }}">Users' registered this month</a></code></li>
        <li><code><a href="{{ path ('last-days-visits') }}">Unique visits : This week statistics</a></code></li>
        <li><code><a href="{{ path ('last-days-visitors') }}">Each user visits per day : This week statistics</a></code></li>
        <li><code><a href="{{ path ('birthday-reminder') }}">Birthday reminder</a></code></li>
    </ul>
    <p>Not in a condition yet?</p>
    <p>Be free to <code><a href="{{ path ('logout') }}">logout</a>, bro</code></p>
</div>
{% endblock %}
